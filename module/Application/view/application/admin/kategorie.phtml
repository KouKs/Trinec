<nav class="shadow">
    <a class="animate" href="">kategorie</a>
    <a class="animate" href="">log</a>
    <a class="animate" href="">schvalování</a>
    <a class="animate" href="">bannery</a>
</nav>

<div class="shadow area">
    <h3>Přidat kategorii</h3>
    <?php echo $this->dir; ?>
    
    <?php   $form = $this->form; 
            $form->prepare();
            echo $this->form()->openTag($form); ?>
    <table class="table-form">
        <tr>
            <td>Název</td>
            <td><?php echo $this->formRow($form->get('nazev')); ?></td>
        </tr>
        <tr>
            <td>Hierarchie</td>
            <td><?php echo $this->formRow($form->get('kategorie')); ?></td>
        </tr>
        <tr>
            <td class="colspan" colspan="2"><?php echo $this->formSubmit($form->get('submit')); ?></td>
        </tr>
    </table>

    <?php echo $this->form()->closeTag(); ?>
</div>
<div class="shadow area">
    <h3>Seznam kategorií</h3>
    <table class="table-stripped table-hover">
        <tr>
            <th>Název</th>
            <th>Rodičovská kategorie</th>
        </tr>
        <?php foreach( $this->kategorie as $row ) : ?>
            <tr>
                <td><?php echo $row["data"]->nazev; ?></td>
                <td><strong>Hlavní kategorie</strong></td>
            </tr>
            <?php if( !empty( $row["children"] ) ) : foreach( $row["children"] as $child ) : ?>
                <tr>
                    <td><?php echo $child["data"]->nazev; ?></td>
                    <td><?php echo $child["parentString"]; ?></td>
                </tr>
            <?php endforeach; endif; ?>
        <?php endforeach; ?>
    </table>
</div>